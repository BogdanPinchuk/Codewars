package org.tasks;

public class StringToNumber {
	public static int stringToNumber(String str) {
		return Integer.parseInt(str);

		// 1 realization
		// Integer.valueOf(str);
		// 2 realization
		// new Scanner(str).nextInt();
	}
}
